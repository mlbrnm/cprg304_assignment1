package utilities;

import shapes.Shape;
import java.util.Comparator;

public class OurSpecialFunSort {

    public static void sort(Shape[] shapes, Comparator<Shape> comparator) {
        if (shapes == null || shapes.length <= 1) {
            return;
        }

        // our custom sort algorithm
    }
}